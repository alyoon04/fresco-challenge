# Anthropic API key (required for extraction)
ANTHROPIC_API_KEY=sk-ant-...

# Postgres
DATABASE_URL=postgresql+psycopg2://fresco:fresco@localhost:5432/fresco

# Redis
REDIS_URL=redis://localhost:6379/0

# Local PDF storage (dev only — use R2 in prod)
UPLOAD_DIR=uploads
